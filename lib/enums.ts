export enum Privacy {
  PUBLIC = 'PUBLIC',
  PRIVATE = 'PRIVATE',
  CLOSED = 'CLOSED',
}

export enum PartyStatus {
  OPEN = 'OPEN',
  FULL = 'FULL',
  CLOSED = 'CLOSED',
  CANCELLED = 'CANCELLED',
}

export enum Role {
  ADMIN = 'ADMIN',
  INDIVIDUAL = 'INDIVIDUAL',
}

export enum Unit {
  GRAMS = 'GRAMS',
  KILOS = 'KILOS',
  QUANTITY = 'QUANTITY',
  MILLILITERS = 'MILLILITERS',
  LITERS = 'LITERS',
  PIECES = 'PIECES',
}
